# Concepts

Planned conceptual documentation from **0.0.1** onward: what an effect is
(and is not), how effects relate to journal events, effect identity,
bounded determinism, and the runtime pipeline
(validate → policy → execute → journal → result).

Status: stub. See `docs/ROADMAP.md` §1–§8 and the ADR index in `docs/adr/`.
