# @agent-effects/replay

Planned in **0.0.3**. Effect-level deterministic replay: resolves effects by
ID from the journal and never re-executes them. `strict` mode first; the
first big demo replays a recorded weather-agent run with zero external
calls. See ADR-0004 (replay semantics).

Status: stub. No code yet — see `docs/ROADMAP.md` §0.0.3 and §8.
