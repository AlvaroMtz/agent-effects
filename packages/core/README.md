# @agent-effects/core

Planned in **0.0.1**. The minimal semantics kernel: `Effect`, `EffectResult`,
`EffectExecutor`, `EffectJournal`, and `EffectRuntime` — no framework
integration, no policy, no CLI. See ADR-0001 (effect vs event), ADR-0002
(effect identity), and ADR-0003 (append-only journal).

Status: stub. No code yet — see `docs/ROADMAP.md` §0.0.1 for scope and exit criteria.
