# @agent-effects/journal-jsonl

Planned in **0.0.2**. Append-only JSONL journal: `cat run.jsonl` must be
useful to a human. Ships with a versioned schema, parser, validator, and
round-trip tests. See ADR-0003 (append-only journal) and ADR-0009 (schema
versioning).

Status: stub. No code yet — see `docs/ROADMAP.md` §0.0.2 for format and exit criteria.
