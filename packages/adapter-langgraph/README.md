# @agent-effects/adapter-langgraph

Planned in **0.0.6**. Second, deliberately different adapter to test whether
the effect IR is genuinely framework-neutral; ships alongside the
conformance suite. Portable fields in core, private details in namespaced
`metadata`. See ADR-0008.

Status: stub. No code yet — see `docs/ROADMAP.md` §0.0.6 and the `conformance/` directory.
