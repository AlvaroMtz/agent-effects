# @agent-effects/journal-memory

Planned in **0.0.1**. In-memory `EffectJournal` reference implementation:
append-only entries with strictly monotonic `sequence` per run. See
ADR-0003 (append-only journal as source of truth).

Status: stub. No code yet — see `docs/ROADMAP.md` §0.0.1 and §0.0.2 for the journal invariants.
