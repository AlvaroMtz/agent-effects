# agentfx (CLI)

Planned in **0.17.0**. The stable command line for recording, replaying,
forking, and diffing runs, e.g. `agentfx replay run.jsonl` and
`agentfx fork run.jsonl --effect fx_2 --result fixtures/rain.json` from the
weather-agent demo.

Status: stub. No code yet — see `docs/ROADMAP.md` §0.17.0 and §23.
