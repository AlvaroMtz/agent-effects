# Specification

Planned in **0.10.0**. Formal split of implementation from specification:
framework-neutral documents for effects, the journal format, replay
semantics, and the adapter contract (`spec/effects.md`, `spec/journal.md`,
`spec/replay.md`, `spec/adapters.md`). From this point independent
implementations in other languages become possible.

Status: stub. No spec text yet — see `docs/ROADMAP.md` §0.10.0.
