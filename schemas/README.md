# Schemas

Planned from **0.0.2** (first versioned journal-entry schema with parser,
validator, and round-trip tests) through **0.10.0** (per-artifact split:
`journal-entry.schema.json`, `effect.schema.json`, `result.schema.json`).
Schemas are the normative reference for all implementations, in any
language. See ADR-0009 (schema versioning).

Status: stub. No schemas yet — see `docs/ROADMAP.md` §0.0.2 and §0.10.0.
