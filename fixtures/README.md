# Fixtures

Planned from **0.0.6** (first compatibility fixtures) to **0.11.0** (the
cross-language suite: `basic-tool-call/`, `model-tool-model/`,
`parallel-tools/`, `failed-tool/`, `retry/`, `human-approval/`, `fork/`).
Any implementation runs these conformance fixtures and may declare itself
compatible. See ADR-0008 and ADR-0009.

Status: stub. No fixtures yet — see `docs/ROADMAP.md` §0.11.0.
